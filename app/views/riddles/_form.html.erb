 <%= form_for @riddle do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.label :answer %>
  <%= f.text_field :answer %>
  <%= f.hidden_field(:user_id, value: session[:user_id] ) %>
   <%= f.submit %>
<% end %>